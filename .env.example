# Signal phone number (required)
# Format: +911234567890 (international format with country code)
SIGNAL_NUMBER=+91XXXXXXXXXX

# Rate limit delay in milliseconds (optional)
# Default: 1000 (1 second between messages)
RATE_LIMIT_DELAY_MS=1000

# Wrapper API port (optional)
# Default: 3000
PORT=3000

# Signal CLI REST API URL (don't change unless needed)
# Default: http://signal-api:8080
SIGNAL_API_URL=http://signal-api:8080
